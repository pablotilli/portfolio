import MobileScreen from '..';
import WallpaperPerferences from '../../Windows/Configuration/WallpaperPreferences';

export default function WallpaperPerferencesScreen() {
  return (
    <MobileScreen>
      <WallpaperPerferences />
    </MobileScreen>
  );
}
